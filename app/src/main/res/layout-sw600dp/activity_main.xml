<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright (c) 2016. Rohan Agarwal (rOhanX96)
  -->
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/background"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background"
        tools:context="com.rohanx96.popularmovies.movieList.MainActivity">
        <include layout="@layout/toolbar"
            android:id="@+id/toolbar_home"/>

        <fragment
            android:id="@+id/movie_list_fragment_container"
            android:name="com.rohanx96.popularmovies.movieList.MovieListFragment"
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:layout_below="@id/toolbar_home"
            tools:layout="@layout/fragment_movie_list">
        </fragment>
        <TextView
            android:id="@+id/movie_detail_select_hint"
            android:layout_below="@id/toolbar_home"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_toRightOf="@id/movie_list_fragment_container"
            android:text="@string/select_movie"
            android:padding="16dp"
            android:textColor="@color/white"
            android:gravity="center"
            android:textAppearance="@style/TextAppearance.AppCompat.SearchResult.Subtitle"/>
        <LinearLayout
            android:id="@+id/movie_detail_fragment"
            android:layout_below="@id/toolbar_home"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_toRightOf="@id/movie_list_fragment_container"
            android:orientation="vertical"/>
    </RelativeLayout>
    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_home"
        app:menu="@menu/nav_menu"
        android:background="@color/background_light"
        app:theme="@style/ThemeOverlay.AppCompat.Dark">
    </android.support.design.widget.NavigationView>

</android.support.v4.widget.DrawerLayout>